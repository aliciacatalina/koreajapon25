node_modules
dist
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.log
.DS_Store

# GitHub Pages build files (these will be in root)
# Don't ignore these since they need to be committed for GitHub Pages
# assets/
# index.html (built version)
